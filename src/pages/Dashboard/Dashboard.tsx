
import { Box } from "@mui/material"
import { Header } from "./Header/Header"

export const Dashboard = () => {
    return (
        <Box sx={{padding: '0 10%'}}>
            <Header/>
        </Box>
    )
}